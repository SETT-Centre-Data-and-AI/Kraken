# Copy file to 'tests/config/credentials.yaml'

main_connection_test:
  # This connection will be used for more extensive testing,
  # and requires write access to a sandbox database. A database
  # must be saved or tests will revert to a temporary DuckDB file,
  # but each connection test will create a copy of the credentials
  # and then remove them, to avoid interfering with saved credentials.
  alias:
  username:
  database:
  schema:
  multi_db: false # turn on if db platform support multiple databases like MSSQL

test_connections: # empty entries will be skipped rather than fail
# platform:
 # alias: ['USERNAME', 'USERNAME'...]
  oracle:

  mssql:

  informix:

  cache:

  mariadb:

  postgresql:
