# Copy files to 'tests/config'

main_connection_test: # This connection will be used for more extensive testing, and requires write access to a sandbox database
  alias: 'ALIAS'
  username: 'USERNAME'
  database: 'DATABASE'
  schema: 'SCHEMA'

test_connections: # empty entries will be skipped rather than fail
# platform:
 # alias: ['USERNAME', 'USERNAME'...]
  oracle:

  mssql:

  informix:

  cache:

  mariadb:

  postgresql:
